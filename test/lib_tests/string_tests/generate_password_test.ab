import {len} from "std"
import * from "../../../lib/string/string.ab"

let r0 = len(generate_password(10))
let r1 = len(generate_password(0))
let r2 = len(generate_password(-1))

echo "{r0} {r1} {r2}"
// 1 -1 -1 -1 -1